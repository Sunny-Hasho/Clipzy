<Window x:Class="Clipzy.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Clipzy" Height="Auto" Width="340"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" SizeToContent="Height"
        WindowStartupLocation="CenterScreen" FontFamily="Neue Montreal, Segoe UI"
        ResizeMode="NoResize">
    <Border Background="White" CornerRadius="12" BorderThickness="1" BorderBrush="#EEE" Margin="10" Padding="20">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="2" Opacity="0.15"/>
        </Border.Effect>
        <StackPanel>
            <!-- Message -->
            <TextBlock x:Name="TxtMessage" Text="Clipzy is already running. Check the system tray." 
                       FontSize="14" Foreground="#333" TextWrapping="Wrap" Margin="0,0,0,20" TextAlignment="Center"/>
            
            <!-- OK Button -->
            <Button x:Name="BtnOk" Click="BtnOk_Click" Background="#333" Foreground="White" 
                    Padding="0,10" BorderThickness="0" Cursor="Hand" FontWeight="SemiBold">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#333"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#444"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                OK
            </Button>
        </StackPanel>
    </Border>
</Window>
