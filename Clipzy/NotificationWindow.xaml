<Window x:Class="Clipzy.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Clipzy" SizeToContent="Height" Width="300"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        FontFamily="Neue Montreal, Segoe UI">
    
    <Window.Resources>
        <!-- Grey Button Style -->
        <Style x:Key="GreyButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#666666"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#000000"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- Border matching TrayMenu -->
    <Border Background="White" CornerRadius="12" BorderThickness="1" BorderBrush="#EEE" Margin="10">
        <Border.Effect>
            <!-- Exact shadow from TrayMenu -->
            <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="2" Opacity="0.1"/>
        </Border.Effect>
        
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock x:Name="TitleText" Text="Clipzy is running" FontWeight="SemiBold" FontSize="14" Foreground="#333" HorizontalAlignment="Center" Margin="0,0,0,5"/>
            
            <!-- Subtext -->
            <TextBlock x:Name="MessageText" Grid.Row="1" Text="Check the system tray icon." Foreground="#888" FontSize="12" Margin="0,0,0,15" HorizontalAlignment="Center"/>

            <!-- Grey Button -->
            <Button Grid.Row="2" Style="{StaticResource GreyButtonStyle}" Click="Button_Click" HorizontalAlignment="Stretch">
                 <TextBlock x:Name="ButtonText" Text="Okay" FontWeight="SemiBold"/>
            </Button>
        </Grid>
    </Border>
</Window>
